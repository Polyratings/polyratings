{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:read && npm run build:write",
    "build:read": "esbuild src/handlers/read-handler.ts --bundle --outdir=dist/handlers",
    "build:write": "esbuild src/handlers/write-handler.ts --bundle --outdir=dist/handlers",
    "test": "jest",
    "lint": "eslint . --ext .ts",
    "prettier-format": "prettier --config .prettierrc 'src/**/*.ts' --write"
  },
  "author": "Addison P. Tustin <addison@atustin.dev>",
  "license": "UNLICENSED",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.3.0",
    "@typescript-eslint/eslint-plugin": "^5.8.0",
    "@typescript-eslint/parser": "^5.8.0",
    "esbuild": "^0.14.8",
    "eslint": "^8.5.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "prettier": "^2.5.1",
    "typescript": "^4.5.4"
  }
}
