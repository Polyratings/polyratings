{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna bootstrap && lerna run build",
    "start": "lerna run start:prod --parallel",
    "start:dev": "lerna run start:dev --parallel"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
